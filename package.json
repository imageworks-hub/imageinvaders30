{
  "name": "imagegames-ios",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/build-mobile.mjs",
    "ios:add": "cap add ios",
    "ios:sync": "npm run build && cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "three": "0.180.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
